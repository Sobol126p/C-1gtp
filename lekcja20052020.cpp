#include <iostream>
#include <fstream>
using namespace std;

int main() 
{
	int licznik = 0;
	string a;
	ifstream plik2;
    plik2.open("c:\\a.txt");  
    if(plik.good())  
    while(!plik2.eof())       
                         {
                         plik2>>a;   
                         tab[]<<a;
						 for (int i=0; i<licznik; i++)
                                 {
	 	                          bool porownanie=licznik<=licznik;
	 	                          if (porownanie)
                                  std::cout <<licznik;
                                 }    
                         }
    plik2.close();
    int t[1000]; 
	t[a] = licznik;
	t[a] = licznik;
	t[a] = licznik;
	t[a] = licznik;
	return 0;
}
