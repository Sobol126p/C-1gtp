#include <iostream>
#include <fstream>    
using namespace std;

int main()
{
	int t[1000];
     plik >> t[i++];
	 
	 for(int i=0; i<ilosc; i++) {  plik2 << t[i]; }
	 
	 string a;     
    ofstream plik;                     
    plik.open("c:\\a.txt");          
	 plik.close();
	  
	for (int i=ilosc-1; i>=0; i--){ 
    ifstream plik2;
    plik2.open("c:\\a.txt");  
    if(plik.good())  
    while(!plik2.eof())        
                         
    plik2.close();
}
    cout<<endl;
    system("PAUSE");
    return 0;
}
